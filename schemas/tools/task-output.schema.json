{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "task-output.schema.json",
  "title": "TaskOutput Metadata — Tier 3 (Minimal)",
  "description": "Minimal metadata for the TaskOutput tool (background task output retrieval). Only source and provenance are carried.",
  "allOf": [{ "$ref": "../base/base-metadata.schema.json" }],
  "required": ["source", "provenance"],
  "notes": { "koth": "NOT applicable.", "decision": "NOT applicable." }
}
