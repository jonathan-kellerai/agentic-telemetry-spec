{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "task-get.schema.json",
  "title": "TaskGet Metadata — Tier 3 (Minimal)",
  "description": "Minimal metadata for the TaskGet tool (single task detail fetch). Only source and provenance are carried.",
  "allOf": [{ "$ref": "../base/base-metadata.schema.json" }],
  "required": ["source", "provenance"],
  "notes": { "koth": "NOT applicable.", "decision": "NOT applicable." }
}
