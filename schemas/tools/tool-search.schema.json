{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "tool-search.schema.json",
  "title": "ToolSearch Metadata — Tier 3 (Minimal)",
  "description": "Minimal metadata for the ToolSearch tool (deferred tool discovery). Only source and provenance are carried.",
  "allOf": [{ "$ref": "../base/base-metadata.schema.json" }],
  "required": ["source", "provenance"],
  "notes": { "koth": "NOT applicable.", "decision": "NOT applicable." }
}
