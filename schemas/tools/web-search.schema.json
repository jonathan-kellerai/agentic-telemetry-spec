{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "web-search.schema.json",
  "title": "WebSearch Metadata — Tier 3 (Minimal)",
  "description": "Minimal metadata for the WebSearch tool. Only source and provenance are carried.",
  "allOf": [{ "$ref": "../base/base-metadata.schema.json" }],
  "required": ["source", "provenance"],
  "notes": { "koth": "NOT applicable.", "decision": "NOT applicable." }
}
