{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "notebook-edit.schema.json",
  "title": "NotebookEdit Metadata — Tier 3 (Minimal)",
  "description": "Minimal metadata for the NotebookEdit tool (Jupyter notebook cell editing). Only source and provenance are carried.",
  "allOf": [{ "$ref": "../base/base-metadata.schema.json" }],
  "required": ["source", "provenance"],
  "notes": { "koth": "NOT applicable.", "decision": "NOT applicable." }
}
